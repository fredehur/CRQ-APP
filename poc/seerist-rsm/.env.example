# Required for live Seerist API calls (otherwise mock mode is used)
SEERIST_API_KEY=

# Required for tools/osint_collector.py live mode (Anthropic API)
ANTHROPIC_API_KEY=

# Required for tools/osint_physical_collector.py live mode
TAVILY_API_KEY=
FIRECRAWL_API_KEY=

# Optional: SMTP for tools/notifier.py live email delivery (otherwise mock)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_FROM=
NOTIFY_TO=
